name: Checks

on:
  pull_request:
    branches: [ master ]
  workflow_call:

jobs:
  lint-fmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/cache-clojure-deps
        with:
          key-label: 'lint'
      - uses: jdx/mise-action@v3
        with:
          install_args: "babashka cljfmt clj-kondo java clojure"
      - name: Lint and format
        run: bb fmt-check && bb deps && bb lint-init && bb lint

  outdated:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/cache-clojure-deps
        with:
          key-label: 'outdated'
      - uses: jdx/mise-action@v3
        with:
          install_args: "babashka java clojure"
      - name: Outdated deps
        run: bb deps && bb outdated-check

  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/cache-clojure-deps
        with:
          key-label: 'tests'
      - name: Setup Chrome
        uses: browser-actions/setup-chrome@latest
        with:
          chrome-version: "140.0.7339.81"
      - uses: jdx/mise-action@v3
        with:
          install_args: "babashka java clojure chromedriver"
      - name: Run tests
        run: bb deps && bb test
